# Port for Node-RED editor + HTTP In nodes (same port by default)
PORT=1880

# Optional: document your PLC on the plant network (set the real IP in the S7 Endpoint in Node-RED UI)
PLC_IP=192.168.0.10
